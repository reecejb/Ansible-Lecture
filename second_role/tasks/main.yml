---
# tasks file for second_role
- name: add several users to a group
  user:
    name: "{{ item }}"
    state: absent
    groups: "wheel"
  loop:
    - testuser1
    - testuser2
    - testuser3
    - testuser4
    - testuser5